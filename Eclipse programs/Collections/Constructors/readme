Constructors in collections are different ways to create a type of collection. 
It can be a default constructor which creates a default collection. There can be another constructor which can be used to create custom
collection with different capacities.
